default:
  outputs:

    dev:
      type: greenplum
      host: [host]
      port: [port]
      user: [username]
      password: [password]
      dbname:  [database]
      schema:  [schema]
  target: dev
